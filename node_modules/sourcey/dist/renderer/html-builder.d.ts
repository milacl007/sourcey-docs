import type { NormalizedSpec } from "../core/types.js";
import type { AlternateLink, RenderOptions, CurrentPage, SiteConfig } from "./context.js";
import type { SiteNavigation } from "../core/navigation.js";
export interface BuildOutput {
    htmlPath: string;
    outputDir: string;
}
export interface SitePage {
    outputPath: string;
    currentPage: CurrentPage;
    spec: NormalizedSpec;
    tabSlug: string;
    pageSlug: string;
    /** Relative path to the generated OG image (set during build) */
    ogImagePath?: string;
    alternateLinks?: AlternateLink[];
}
export declare function createRenderOptions(page: SitePage, site: SiteConfig, options?: {
    embeddable?: boolean;
    assetBase?: string;
}): RenderOptions;
/**
 * Build a documentation site.
 */
export declare function buildSite(pages: SitePage[], navigation: SiteNavigation, outputDir: string, site: SiteConfig, options?: {
    embeddable?: boolean;
    searchIndex?: string;
    llmsTxt?: string;
    llmsFullTxt?: string;
    ogImages?: Map<string, Buffer>;
    extraFiles?: Map<string, string | Buffer>;
}): Promise<BuildOutput>;
//# sourceMappingURL=html-builder.d.ts.map