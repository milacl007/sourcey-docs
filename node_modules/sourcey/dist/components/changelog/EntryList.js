import { jsx as _jsx } from "preact/jsx-runtime";
export function EntryList({ entries }) {
    if (!entries.length)
        return null;
    return (_jsx("ul", { class: "sourcey-changelog-entry-list", children: entries.map((entry, index) => (_jsx("li", { dangerouslySetInnerHTML: { __html: entry.html } }, `${entry.text}-${index}`))) }));
}
